# Python dependencies. Verified on Odoo 17/18/19 with Python 3.12.
langchain>=0.3
langchain-core>=0.3
langchain-community>=0.3
langchain-ollama>=1.0        # ChatOllama — the local Ollama provider (default)
langgraph>=0.2
bleach>=6.0                  # sanitises the model's HTML output
markdown>=3.5                # renders the model's Markdown (tables/code) before sanitising

# Amazon Bedrock provider. Only needed if the AI Provider is set to "bedrock";
# the import is lazy, so it can be omitted for an Ollama-only install.
langchain-aws>=0.2

# langgraph needs a newer typing_extensions than some base images ship.
typing_extensions>=4.13
